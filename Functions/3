# Exercise 3: Function with Multiple Parameters
# 1. Define a function calculate_area(length, width) that returns the area of a rectangle. Call it with different values.
# 2. Create a function is_even(number) that returns True if the given number is even, and False otherwise. Test it with different numbers.


def calculate_area(a, b):
    return a*b



def is_even(x):
    if x % 2 == 0:
        return True
    else:
        return False
    
----------------------
---ChatGPT---
print(calculate_area(4, 2))

print(is_even(40))

def calculate_area(a, b):
    return a * b

def is_even(x):
    # You can return the result of the condition directly
    return x % 2 == 0

# Testing the functions
print("Area:", calculate_area(4, 2))    # Output: Area: 8
print("Is Even:", is_even(40))         # Output: Is Even: True

In Python, the expression x % 2 == 0 itself is a boolean expression that results in either True or False. Thus, you don’t need to use an if-else statement to return True or False explicitly.






